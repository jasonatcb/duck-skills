---
name: faq-generator
description: 為文件新增常見問題 (FAQ) 區塊。當用戶說「為某個文檔添加 FAQ」、「在文件中加入常見問題」、「生成 FAQ」或類似意圖時觸發此技能。使用方式：提供文檔路徑（如 docpath.md），技能會分析文檔內容，生成 2-3 個與主題相關的常見問題，並使用 zensical quote callout 格式添加到 ## 常見問題區塊中。
version: "1.0.0"
last_updated: 2026-03-18
---

# FAQ 生成技能

此技能會根據文檔內容生成常見問題 (FAQ) 並添加到文件中。

## 使用方式

用戶提供文檔路徑作為參數，例如：`為 docpath.md 添加 FAQ`

## 執行步驟

### 1. 讀取文檔內容
- 讀取用戶指定的文檔路徑
- 分析文檔的主要內容、重點功能、設定項目

### 2. 生成相關的常見問題
根據文檔內容，生成 2-3 個合理的常見問題：
- 問題應該與文檔主題直接相關
- 答案應該基於文檔中提到的功能或設定
- 避免生成文檔未提及的內容

### 3. 添加 FAQ 到文檔
使用 zensical quote callout 格式：

```markdown
## 常見問題

??? quote "問題文字"

    答案內容（縮排）
```

**格式規則：**
- 使用 `??? quote` 開頭
- 問題文字放在雙引號內
- 答案內容縮排 4 個空格
- 每個 FAQ 之間用空行分隔

### 4. 更新 last_modified
如果文檔有 frontmatter，請更新 `last_modified` 欄位為當前時間（格式：YYYY-MM-DD HH:mm）。

## 範例

**輸入文檔：** `docs/ec/payments/設定信用卡.md`
（內容關於如何設定信用卡付款方式）

**輸出：**

```markdown
## 常見問題

??? quote "支援哪些信用卡種類？"

    目前支援 Visa、Mastercard、JCB、美國運通等主流信用卡。

??? quote "分期付款最多可以分幾期？"

    最長可支援 12 期分期，具体期数视银行方案而定。
```

## 注意事項

- 如果文檔中已有 `## 常見問題` 區塊，在現有區塊下方新增
- 如果沒有 `## 常見問題` 區塊，在文檔末尾（frontmatter 之後）新增
- 確保每個 FAQ 的答案簡潔明瞭，不超過 3-4 個要点